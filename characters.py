# Import characters library
import string

# Create all the characters
letters = string.ascii_letters
numbers = string.digits
special = string.punctuation
characters = letters + numbers + special
print(characters)
